I want you to improve the comments and documentation of this entire C++ game engine project.

IMPORTANT:
- Do NOT change the behavior of the program.
- Do NOT refactor the architecture.
- Do NOT rename classes, functions, variables, or files.
- Do NOT change algorithms unless absolutely necessary to add a comment.
- Do NOT add comments to every line.
- Do NOT write comments that simply describe what the code literally does.
- Focus on explaining the PURPOSE, INTENT, RESPONSIBILITY, and IMPORTANT DESIGN DECISIONS behind the code.
- Preserve my existing coding style.

This project is a custom C++ game engine/game project, and the code will be shown as a portfolio to professional game programmers.

The goal is to address professional code-review feedback that my source code does not contain enough comments and that it can be difficult for another programmer or my future self to understand what each system is doing.

Review the ENTIRE repository and add useful comments where they provide meaningful context.

Prioritize these areas:

1. IMPORTANT CLASSES
For major systems and classes, add a short class-level comment explaining:
- What the class is responsible for
- What role it plays in the engine
- How it relates to other systems when this is important

2. IMPORTANT FUNCTIONS
For non-trivial functions, add comments explaining:
- What the function is responsible for
- Important inputs/outputs
- Important side effects
- Any non-obvious behavior

Do NOT add comments to trivial getters, setters, constructors, or obvious one-line functions unless there is something important to explain.

3. COMPLEX LOGIC
For complicated code, explain WHY the implementation works this way.

Especially comment:
- AI decision making
- Behaviour Tree logic
- Blackboard communication
- State transitions
- Collision detection
- Physics calculations
- Camera calculations
- Rendering
- Shader-related logic
- Object creation
- Factory logic
- Component communication
- Resource management
- Scene management
- Any custom engine architecture

4. DESIGN INTENT
When the code uses a particular design or architecture, explain the reason.

For example:

BAD:
    // Add velocity to position

GOOD:
    // Apply the current velocity to the object's position.
    // Movement is updated here so that all movement-related
    // calculations are processed consistently during the update cycle.

If a system uses a Blackboard, explain WHY information is separated into the Blackboard rather than simply saying:
    // Store target

5. BEHAVIOUR TREE
Pay special attention to the BehaviourTree directory.

For each important node, make it clear:
- What condition/action it represents
- What causes Success
- What causes Failure
- What causes Running, if applicable
- How it interacts with the Blackboard or other AI systems

6. BLACKBOARD
Document:
- What kind of information is stored
- How AI nodes use the information
- Why this information is separated from individual Behaviour Tree nodes

7. STATES
Document important state classes and transitions.

Explain:
- What the state represents
- When the state becomes active
- What causes it to transition to another state
- Any important conditions

8. FACTORY / COMPONENT / ENGINE SYSTEMS
Document the responsibility of important systems and why the separation exists.

9. CONSTANTS AND MAGIC NUMBERS
DO NOT automatically refactor everything.
If you find a magic number whose meaning is unclear, add a comment identifying its purpose.

Example:
    // Maximum distance at which the target can be detected.
    constexpr float MAX_DETECTION_DISTANCE = 10.0f;

Only introduce a named constant if doing so is obviously safe and does not change program behavior. Otherwise, leave the code unchanged and report it separately.

10. COMMENTS MUST BE CONSISTENT
Use consistent terminology throughout the project.

Do not mix different names for the same concept.

For example, if the project calls something "Target", do not sometimes call it "Enemy", "Opponent", or "Object" unless they actually mean different things.

COMMENT STYLE:
- Use clear, concise English.
- Write comments that a professional C++ game programmer would consider useful.
- Avoid excessive comments.
- Avoid explaining obvious syntax.
- Avoid comments that become false if the implementation changes slightly.
- Avoid repeating the function name in sentence form.
- Prefer explaining WHY and DESIGN INTENT over WHAT the code literally does.

PORTFOLIO QUALITY:
The final code should make it easy for a programmer reviewing my portfolio to understand the architecture and identify the important systems.

Do NOT modify the README yet.
Do NOT create a separate documentation file yet.

FIRST:
1. Scan the entire repository.
2. Identify the major systems and their relationships.
3. Identify files/functions that require comments.
4. Add the comments directly to the source code.
5. Keep the existing implementation unchanged.
6. At the end, provide a summary containing:
   - Files modified
   - Major systems documented
   - Any areas that still need improvement
   - Any magic numbers or maintainability issues you noticed
   - The 5 strongest parts of the codebase that would be good candidates to highlight in a portfolio

Before making changes, show me a short plan of which major systems you identified and where you intend to add comments. Do not modify files until I approve the plan.