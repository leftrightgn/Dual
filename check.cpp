#include <windows.h>
#include <iostream>
#include <string>

int main() {
    std::cout << "Please run the game and check Visual Studio Output window for the 'Failed to load animation' error." << std::endl;
    return 0;
}
